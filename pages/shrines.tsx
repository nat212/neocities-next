import { NextPage } from 'next';
import Layout from '@components/layout';
import PageHeader from '@components/page-header';

const Shrines: NextPage = () => {
    return (
        <Layout pageTitle="Shrines!">
            <PageHeader title="Shrines!" />
        </Layout>
    );
};

export default Shrines;
